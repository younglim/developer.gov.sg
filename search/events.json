---
---

{
    "pages": [
        {%- for document in site["communities"] -%}
            {%- assign target_group_joined = document.target_group | join: ", " -%}
            {%- if document.url contains '/conferences/' or document.url contains '/stack-x-meetups/' -%}
                {%- if document.url contains 'overview.html' or document.url contains 'index.html' -%}
                {
                    "title": "{{ document.title | remove: '"' }}",
                    "description": "{{ document.description | remove: '>' | remove: '"' | strip }}",
                    "targetGroup": "{{ target_group_joined }}",
                    "category": "{{ document.category }}",
                    "content": {{ document.content | strip_html | strip_newlines | strip | jsonify }},
                    "url": "{{ document.url }}",
                    "meta": "document"
                },
                {%- endif -%}
            {%- endif -%}
            {%- if forloop.last -%}{}{%- endif -%}
        {%- endfor -%}
    ]
}